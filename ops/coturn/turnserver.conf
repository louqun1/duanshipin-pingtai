# Stage 0 coturn config for probe validation.
# Replace the static test credential before exposing this server to the public internet.

listening-port=3478
fingerprint

lt-cred-mech
realm=duanshipin.local
user=probe:probe_turn_123
listening-ip=0.0.0.0
relay-ip=192.168.3.28
external-ip=192.168.3.28

min-port=49160
max-port=49200

no-tls
no-dtls
cli-password=probe_cli_123
simple-log
log-file=stdout

# If the VM later sits behind a public NAT, replace with:
# external-ip=<public-ip>/<private-ip>
