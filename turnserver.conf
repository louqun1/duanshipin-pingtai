# Stage 0 coturn config for probe validation.
# Replace the static test credential before exposing this server to the public internet.

listening-port=3478
fingerprint

lt-cred-mech
realm=duanshipin.local
user=probe:probe_turn_123

min-port=49160
max-port=49200

no-cli
simple-log
log-file=stdout

# If the VM is behind a public NAT later, add:
# external-ip=<public-ip>/<private-ip>
