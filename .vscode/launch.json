{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Qt Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/QtFrontend.exe",
            "cwd": "${workspaceFolder}/build/bin",
            "MIMode": "gdb"
        }
    ]
}
